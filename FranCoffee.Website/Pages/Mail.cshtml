@page
@model FranCoffee.Website.Pages.MailModel
@{
}


<section class="ftco-section mt-5">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-10">
                <h2 class="text-center">
                    @if (Model.Success)
                    {
                        @Model.L["success"]
                    }
                    else
                    {
                        @Model.L["fail"]
                    }
                </h2>
            </div>
        </div>
    </div>
</section>
